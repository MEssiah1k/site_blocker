# Edge 自律型网站屏蔽插件 产品需求文档（PRD + Technical Spec）

## 1. 文档目的

本文档用于定义一个运行在 **Microsoft Edge（Chromium 内核）** 上的浏览器扩展，用于对指定网站进行高强度自律式访问控制。

该文档面向：
- 产品设计
- 工程实现
- 使用 Codex / LLM 辅助编码
- 后续功能迭代

文档内容覆盖：
- 产品目标
- 功能逻辑
- 边界条件
- 页面交互
- 状态机
- 数据结构
- 技术实现建议
- 非目标范围
- 风险与限制

---

## 2. 产品定位

### 2.1 产品类型

这是一个 **强制型自律工具**，不是普通的“网站提醒器”或“内容过滤器”。

核心特点：
- 默认阻断指定网站访问
- 用户不能轻易绕过
- 对部分网站允许“高摩擦临时申请”
- 对部分网站完全禁止访问
- 不提供低摩擦的一键关闭能力

### 2.2 产品目标

帮助用户减少冲动访问、娱乐性分心和低价值浏览。

### 2.3 核心设计原则

1. **默认禁止，例外放行**
2. **解除必须有成本**
3. **产品逻辑应简单明确，不做隐性判断**
4. **允许用户动态配置规则**
5. **状态必须持久化，不能依赖页面内存**
6. **不做访问统计，不做轻提醒模式**

---

## 3. 产品范围

### 3.1 本期必须实现

1. 用户可以添加 / 删除 / 编辑网站规则
2. 网站规则分为三类：
   - 完全不允许使用
   - 有时需要大量使用
   - 有时偶尔使用
3. 命中规则后默认跳转到插件内置阻断页
4. 完全不允许使用的网站不可申请放行
5. 其余两类网站可申请临时使用
6. 申请临时使用时：
   - 用户输入申请时间
   - 系统根据时间和网站等级生成随机验证码
   - 用户必须手动输入验证码
   - 禁止粘贴
7. 验证通过后，在指定时长内允许访问目标网站
8. 到期后自动恢复阻断
9. 插件必须基于 Edge 扩展实现
10. 插件规则和临时授权状态需要本地持久化

### 3.2 本期明确不做

1. 不做访问统计
2. 不做云同步
3. 不做多设备同步
4. 不做账号系统
5. 不做密码找回
6. 不做家长控制
7. 不做 AI 分类网站
8. 不做网页内容级屏蔽（例如只屏蔽某个页面元素）
9. 不做复杂报表

---

## 4. 用户故事

### 4.1 作为用户，我希望

- 我能将 YouTube、Bilibili、Twitter/X 等站点加入限制列表
- 我能给不同网站设置不同等级
- 当我访问被限制的网站时，浏览器不会直接进入原页面，而是跳到一个阻断页面
- 如果该网站属于“可申请临时使用”的类别，我可以申请一段时间的访问权限
- 申请时间越长，我输入的验证码越复杂
- 验证码必须自己手打，不能复制粘贴
- 如果该网站属于“完全禁止”，则没有任何放行入口
- 申请成功后，我只在限定时间内可以访问该站点，时间结束后再次拦截
- 我可以通过插件 UI 管理这些规则

---

## 5. 核心概念定义

### 5.1 网站分类（策略等级）

系统对被限制网站分为三类：

#### Level 0：完全不允许使用
- 默认阻断
- 永远不可申请临时访问
- 访问时仅展示阻断页和提示信息

#### Level 1：有时需要大量使用（受控使用）
- 默认阻断
- 可申请临时放行
- 放行成本高
- 申请时间增加得越多，验证码长度增长越快

#### Level 2：有时偶尔使用（偶尔使用）
- 默认阻断
- 可申请临时放行
- 放行成本相对较低

### 5.2 匹配粒度

本期只支持 **粗粒度整站匹配**，即按域名匹配。

示例：
- `youtube.com`
- `x.com`
- `bilibili.com`

匹配规则要求：
- 命中主域名即视为命中整个站点
- 所有子路径都算命中
- 视需要支持子域名一起命中，例如：
  - `www.youtube.com`
  - `m.youtube.com`
  - `youtube.com/watch...`

### 5.3 时间申请

用户申请临时放行时，需要输入目标使用时长，单位为分钟。

最小允许申请时间：
- `5 min`

不允许小于 5 分钟。

---

## 6. 功能逻辑总览

### 6.1 访问流程总览

```text
用户访问 URL
  ↓
插件检测当前 URL 是否命中限制规则
  ↓
若未命中 → 正常放行
  ↓
若命中 → 检查该域名是否存在有效的临时放行记录
  ↓
若存在且未过期 → 放行
  ↓
否则 → 跳转到阻断页
```

### 6.2 阻断页行为

根据网站级别执行不同逻辑：

- Level 0：仅显示禁止访问提示，不提供任何申请入口
- Level 1 / Level 2：显示申请使用表单

### 6.3 申请流程

```text
用户在阻断页输入申请时长
  ↓
系统根据网站等级 + 申请时长 计算验证码长度
  ↓
系统生成随机验证码
  ↓
展示验证码
  ↓
用户手动输入验证码（禁止粘贴）
  ↓
验证成功 → 写入临时放行状态
  ↓
跳转回目标网站
```

---

## 7. 验证码难度函数

### 7.1 基础参数

- 基础长度：`5`
- 基础时间：`5 min`
- 最小可申请时间：`5 min`

### 7.2 Level 1 规则

Level 1 表示“有时需要大量使用”，增长更陡。

规则：
- 申请时间 5 分钟时，验证码长度为 5
- 在此基础上，每额外增加 2 分钟，验证码长度增加 5 个字符

公式：

```text
L = 5 + 5 * floor((T - 5) / 2)
```

其中：
- `L` = 验证码长度
- `T` = 申请时间（分钟）

示例：

| 申请时间 T | 额外时间 | 增量段数 | 长度 L |
|---|---:|---:|---:|
| 5 | 0 | 0 | 5 |
| 6 | 1 | 0 | 5 |
| 7 | 2 | 1 | 10 |
| 9 | 4 | 2 | 15 |
| 11 | 6 | 3 | 20 |
| 15 | 10 | 5 | 30 |

### 7.3 Level 2 规则

Level 2 表示“有时偶尔使用”，增长较缓。

规则：
- 申请时间 5 分钟时，验证码长度为 5
- 在此基础上，每额外增加 10 分钟，验证码长度增加 5 个字符

公式：

```text
L = 5 + 5 * floor((T - 5) / 10)
```

示例：

| 申请时间 T | 额外时间 | 增量段数 | 长度 L |
|---|---:|---:|---:|
| 5 | 0 | 0 | 5 |
| 10 | 5 | 0 | 5 |
| 15 | 10 | 1 | 10 |
| 25 | 20 | 2 | 15 |
| 35 | 30 | 3 | 20 |

### 7.4 必须使用向下取整

实现时必须使用 `floor`，不能使用四舍五入。

### 7.5 时间输入约束

- 必须为整数分钟
- 最小值为 5
- 建议设置最大值，例如 180 分钟，防止极端输入

### 7.6 最大长度建议

虽然当前需求未强制要求上限，但为了避免超长随机码导致 UI 和体验失控，建议增加一个安全上限，例如：

- 最大验证码长度：`100`

若用户申请时长导致长度超过上限，则取上限值。

这项建议可设为可配置项，默认开启。

---

## 8. 验证码规则

### 8.1 字符集要求

验证码字符集必须包含：
- 数字：`0-9`
- 大写英文字母：`A-Z`
- 小写英文字母：`a-z`
- 特殊符号：`!@#$%^&*()`

完整字符集可定义为：

```text
ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()
```

### 8.2 验证码生成原则

1. 长度由难度函数决定
2. 验证码必须随机生成
3. 建议至少包含以下每类字符至少 1 个：
   - 1 个大写
   - 1 个小写
   - 1 个数字
   - 1 个特殊符号
4. 剩余字符从全字符集随机补足
5. 最后对结果执行随机洗牌

### 8.3 随机源要求

必须使用浏览器提供的加密级随机源：
- `crypto.getRandomValues()`

不要使用：
- `Math.random()`

原因：
- `Math.random()` 的随机性较弱且可预测性更高

### 8.4 验证码生命周期

- 验证码只在当前申请流程中有效
- 用户刷新阻断页后，验证码应重新生成或重新开始申请流程
- 验证码不需要长期持久化

---

## 9. 输入限制与防作弊要求

### 9.1 禁止粘贴

验证码输入框必须禁止粘贴：
- 屏蔽 `paste` 事件
- `event.preventDefault()`

### 9.2 禁止拖拽输入

建议屏蔽以下行为：
- `drop`
- `dragenter`
- `dragover`

避免通过拖拽文本绕过手输限制。

### 9.3 禁止自动填充

建议设置：
- `autocomplete="off"`
- `autocorrect="off"`
- `autocapitalize="off"`
- `spellcheck="false"`

### 9.4 不要求防脚本注入

由于这是本地插件页面，普通用户主要绕过路径不是脚本注入，而是：
- 粘贴
- 一键复制
- 关闭插件

因此本期无需实现复杂的“按键节奏分析”或“机器输入检测”。

### 9.5 用户可见提示

当用户尝试粘贴时，建议给出明确提示：
- “验证码必须手动输入，禁止粘贴”

---

## 10. 状态机设计

### 10.1 网站访问状态

对于任一被限制网站，运行时状态可以抽象为：

- `BLOCKED`：默认阻断
- `TEMP_ALLOWED`：临时放行中

如果需要更细状态，也可在 UI 层定义：
- `IDLE`：尚未开始申请
- `CODE_GENERATED`：验证码已生成，等待输入
- `VERIFIED`：验证成功

### 10.2 状态流转

```text
访问网站
  ↓
命中规则
  ↓
检查临时放行记录
  ├─ 有效 → TEMP_ALLOWED → 放行
  └─ 无效 → BLOCKED → 跳转阻断页

阻断页内：
BLOCKED
  ↓ 点击申请
生成验证码
  ↓
输入正确
  ↓
写入临时放行记录
  ↓
TEMP_ALLOWED
  ↓
时间到期
  ↓
BLOCKED
```

### 10.3 状态判定原则

状态必须以持久化数据为准，不能只靠页面状态。

也就是说：
- 关闭标签页后再次访问，仍应检查本地存储中的授权是否有效
- 刷新网页后，仍应正确判定是否放行

---

## 11. 规则管理功能

### 11.1 支持的操作

用户应能通过插件 UI 对规则进行：
- 新增网站规则
- 编辑网站规则
- 删除网站规则
- 启用/禁用某条规则

### 11.2 单条规则字段

每条规则至少包含：

- 域名 `domain`
- 等级 `level`
- 是否启用 `enabled`
- 可选备注 `note`
- 创建时间 `createdAt`
- 更新时间 `updatedAt`

### 11.3 域名输入规范

本期建议只接受域名，不接受完整 URL。

例如：
- 合法：`youtube.com`
- 合法：`x.com`
- 非法：`https://youtube.com/watch?v=...`
- 非法：`/watch?v=...`

建议 UI 中对输入做规范化：
- 去掉前后的空格
- 自动转小写
- 去掉协议头 `http://` / `https://`
- 去掉尾部 `/`
- 若输入包含路径，仅保留 host，或直接报错要求重新输入

### 11.4 重复规则处理

若用户重复添加相同域名：
- 不新增新记录
- 提示“该域名已存在”
- 可引导用户直接编辑现有规则

### 11.5 启用/禁用语义

- `enabled = true`：规则生效
- `enabled = false`：规则存在，但不参与拦截

---

## 12. 阻断页功能需求

### 12.1 阻断页定位

阻断页是插件核心交互页面。

当用户访问被拦截网站时，应跳转到插件内置页面，例如：

```text
chrome-extension://<extension-id>/blocked.html?target=...
```

### 12.2 阻断页需展示的信息

必须展示：
- 当前被拦截的网站域名
- 该网站当前所属等级
- 当前访问被阻止的说明

对于可申请的网站，还应展示：
- 申请时长输入框
- 生成验证码按钮
- 当前生成的验证码展示区域
- 验证码输入框
- 提交按钮

### 12.3 Level 0 页面要求

若命中 Level 0：
- 不显示申请时长输入
- 不显示生成验证码按钮
- 不显示验证码输入框
- 只展示“此网站被永久禁止访问”之类的说明

### 12.4 Level 1 / Level 2 页面要求

必须支持：
1. 输入申请时间
2. 点击“生成验证码”
3. 展示验证码
4. 手动输入验证码
5. 点击“确认申请”
6. 成功后跳回目标网站

### 12.5 建议的交互文案

文案可后续调整，但建议保留足够直白的语气，例如：
- “你正在尝试访问受限网站”
- “如果确实需要使用，请申请临时访问权限”
- “申请时间越长，需要手动输入的验证码越长”
- “验证码必须手动输入，禁止粘贴”

---

## 13. 临时放行机制

### 13.1 临时放行定义

临时放行表示：
- 某个被限制域名在某段时间内被允许访问
- 到期后自动恢复拦截

### 13.2 授权粒度

本期建议按 **域名级** 授权，而不是 URL 级。

例如：
- 对 `youtube.com` 授权 15 分钟
- 在授权有效期内，访问 `www.youtube.com`、`youtube.com/watch...` 均视为放行

### 13.3 授权数据核心字段

- `domain`
- `startAt`
- `expireAt`

### 13.4 授权判定逻辑

当访问命中规则域名时：
- 若存在未过期临时授权，则放行
- 若授权已过期，则应清理或视为无效

### 13.5 同域名多次申请处理

建议逻辑：
- 新申请覆盖旧授权
- 以最新的 `expireAt` 为准

也可以考虑“延长授权”策略，但本期为保持简单，建议直接覆盖。

---

## 14. 数据存储设计

### 14.1 存储方案

使用浏览器扩展本地存储：
- `chrome.storage.local`

理由：
- 满足本地持久化需求
- 无需云端
- 可在 service worker、popup、阻断页之间共享

### 14.2 建议数据结构

```json
{
  "rules": [
    {
      "id": "rule_001",
      "domain": "youtube.com",
      "level": 1,
      "enabled": true,
      "note": "视频站",
      "createdAt": 1710000000000,
      "updatedAt": 1710000000000
    },
    {
      "id": "rule_002",
      "domain": "x.com",
      "level": 0,
      "enabled": true,
      "note": "完全禁止",
      "createdAt": 1710000000000,
      "updatedAt": 1710000000000
    }
  ],
  "temporaryAccess": [
    {
      "domain": "youtube.com",
      "startAt": 1710000100000,
      "expireAt": 1710001000000,
      "grantedAt": 1710000100000
    }
  ],
  "settings": {
    "maxRequestMinutes": 180,
    "maxCaptchaLength": 100
  }
}
```

### 14.3 规则 level 编码建议

- `0` = 完全不允许使用
- `1` = 有时需要大量使用
- `2` = 有时偶尔使用

### 14.4 为什么不把 temporaryAccess 设计成 map

也可以设计为：

```json
{
  "temporaryAccess": {
    "youtube.com": {
      "startAt": 1710000100000,
      "expireAt": 1710001000000
    }
  }
}
```

这在查找时更高效。若工程上更看重访问效率而不是数组遍历，可优先采用 map 结构。

推荐最终形式：

```json
{
  "temporaryAccess": {
    "youtube.com": {
      "startAt": 1710000100000,
      "expireAt": 1710001000000,
      "grantedAt": 1710000100000
    }
  }
}
```

---

## 15. 页面与模块划分

### 15.1 页面建议

至少包含：

1. `popup.html`
   - 点击扩展图标后打开
   - 用于规则管理

2. `blocked.html`
   - 被限制网站跳转到此页面
   - 用于展示阻断信息和申请临时放行

### 15.2 JS 模块建议

建议拆分为以下模块：

1. `background.js` / `service_worker.js`
   - 负责监听导航
   - 判定是否拦截
   - 跳转到阻断页

2. `popup.js`
   - 规则管理界面逻辑

3. `blocked.js`
   - 阻断页逻辑
   - 申请流程
   - 验证码生成与校验

4. `storage.js`
   - 封装本地存储读写

5. `rule-engine.js`
   - 规则匹配
   - level 判定
   - 临时授权判定

6. `captcha.js`
   - 验证码长度计算
   - 验证码生成

7. `utils.js`
   - 域名规范化
   - 时间计算
   - query 参数解析

---

## 16. 技术实现建议（Edge 扩展）

### 16.1 扩展规范

建议使用：
- `Manifest V3`

因为 Edge 基于 Chromium，当前扩展体系与 Chrome 基本一致。

### 16.2 核心权限建议

可能需要的权限：
- `storage`
- `tabs`
- `webNavigation`
- `scripting`
- `declarativeNetRequest`（视最终方案决定）
- `declarativeNetRequestWithHostAccess`（如使用 DNR）

以及主机权限：
- `<all_urls>` 或用户规则相关 host 权限

### 16.3 推荐拦截方式

本项目更适合通过 **导航监听 + 主动重定向** 方式实现，而不是只靠静态规则文件。

原因：
1. 规则是动态可配置的
2. 需要在重定向前读取本地规则和临时授权状态
3. 需要按运行时状态放行

推荐实现方向：
- 使用 `chrome.webNavigation.onBeforeNavigate` 或相关导航事件监听主框架导航
- 判断当前 URL 是否命中规则
- 若需阻断，则更新标签页到 `blocked.html?target=...`

### 16.4 只拦截主页面，不拦截资源请求

本期目标是阻断用户打开网站，而不是过滤页面内资源。

因此建议只处理：
- `main_frame` 级导航

不要拦截：
- 图片
- 脚本
- CSS
- iframe 内嵌资源（除非后续发现有绕过场景）

### 16.5 关于 declarativeNetRequest

`declarativeNetRequest` 适合静态拦截或简单动态规则，但对“根据本地授权状态决定是否放行”的逻辑不够灵活。

因此：
- 可以不用 DNR 作为主方案
- 若后续需要更强的底层屏蔽，可将 DNR 作为增强层

### 16.6 service worker 职责

扩展后台 service worker 负责：
- 加载规则
- 解析访问 URL
- 匹配域名
- 查询临时授权
- 决定是否跳转阻断页

### 16.7 阻断页回跳

`blocked.html` 中需要能读取目标网址参数：
- `target`
- 可选 `domain`

验证通过后：
- 直接 `location.href = target`
- 或通知 background 统一处理导航

推荐优先使用直接跳转，逻辑更简单。

---

## 17. 域名匹配规则

### 17.1 基本匹配要求

若规则域名为 `youtube.com`，则以下都应命中：
- `youtube.com`
- `www.youtube.com`
- `m.youtube.com`
- `youtube.com/watch?v=...`

### 17.2 推荐匹配逻辑

解析目标 URL 的 hostname，设为 `host`。

若存在规则域名 `domain`，则命中条件可定义为：

```text
host === domain
或
host 以 '.' + domain 结尾
```

示例：
- `www.youtube.com` 以 `.youtube.com` 结尾 → 命中
- `notyoutube.com` 不应命中

### 17.3 规范化处理

规则域名和访问域名都应：
- 转为小写
- 去掉首尾空格

---

## 18. 详细功能规格

## 18.1 规则管理页面（popup）

### 页面目标

让用户能快速维护规则列表。

### 页面元素

1. 新增规则表单
   - 域名输入框
   - 等级选择下拉框 / 单选按钮
   - 启用开关
   - 备注输入框（可选）
   - 保存按钮

2. 规则列表
   - 域名
   - 等级
   - 启用状态
   - 编辑按钮
   - 删除按钮

### 交互要求

#### 新增规则
- 点击保存后校验输入
- 域名非法则报错
- 域名重复则报错
- 成功后加入列表

#### 编辑规则
- 支持修改：
  - 域名
  - 等级
  - 启用状态
  - 备注
- 保存后更新时间戳

#### 删除规则
- 删除前可加确认弹窗
- 删除后立即生效
- 同时建议清理该域名的临时授权记录

#### 启用/禁用切换
- 修改后立即生效

---

## 18.2 阻断页（blocked）

### 入参

通过 query 参数传入：
- `target`：原始目标 URL
- `domain`：命中的规则域名（可选，若 background 已计算）

### 页面初始化逻辑

1. 读取 query 参数
2. 解析目标 host
3. 从 storage 查询对应规则
4. 若规则不存在，说明状态不同步，可给出错误提示并提供返回原页面按钮
5. 若规则存在，按 level 渲染 UI

### 申请表单逻辑

#### 输入时长
- 只能输入正整数
- 小于 5 时提示错误
- 大于最大值时提示错误

#### 点击生成验证码
执行：
1. 校验时长
2. 根据 level 计算长度
3. 生成验证码
4. 渲染验证码
5. 显示输入框

#### 点击确认申请
执行：
1. 比较输入与当前验证码
2. 若不相等，提示失败
3. 若相等：
   - 计算授权起止时间
   - 写入 storage
   - 跳转到目标页面

### 页面刷新行为

刷新后建议：
- 清空本次验证码状态
- 要求重新生成验证码

原因：
- 流程更清晰
- 降低通过刷新操作保留旧验证码的复杂度

---

## 19. 错误处理与边界条件

### 19.1 用户输入非法时间

情况：
- 空值
- 非数字
- 小数
- 小于 5
- 大于上限

处理：
- 阻止继续
- 提示用户输入合法整数分钟数

### 19.2 用户未先生成验证码就点击确认

处理：
- 提示“请先生成验证码”

### 19.3 验证码输入错误

处理：
- 明确提示输入错误
- 不自动生成新验证码
- 用户可继续重输或手动重新生成

### 19.4 存储读取失败

处理：
- 提示“本地数据读取失败”
- 禁止继续流程

### 19.5 目标 URL 缺失

阻断页若缺少 `target` 参数：
- 显示错误状态
- 不允许申请

### 19.6 规则删除后的残留授权

若某域名规则被删除：
- 建议同时删除对应 temporaryAccess
- 避免脏数据堆积

### 19.7 时间到期时正在页面内

本期建议逻辑：
- 到期后**不强制立即踢出当前已打开页面**
- 但用户刷新、跳转、重新打开时会再次拦截

理由：
- 实现更简单
- 不需要持续注入页面脚本轮询

若未来想做更强控制，可加“到点强制中断当前 tab”。

---

## 20. 非功能需求

### 20.1 性能

- 规则匹配应足够轻量
- 单次导航判定应尽量在毫秒级完成
- 规则数量通常不会太大（几十到几百），本期无需复杂索引

### 20.2 可维护性

- 业务逻辑与 UI 分离
- storage 操作统一封装
- 验证码逻辑单独模块化
- 域名匹配单独模块化

### 20.3 可测试性

建议将以下逻辑写成纯函数并单元测试：
- 域名规范化
- 域名匹配
- 验证码长度计算
- 验证码生成
- 临时授权是否过期判断

### 20.4 安全性

这是本地自律工具，不是防攻击系统。

目标是：
- 提高绕过成本
- 降低冲动访问概率

不是：
- 绝对防破解
- 绝对防卸载

---

## 21. 已知限制

### 21.1 无法防止用户禁用或卸载扩展

这是浏览器扩展权限模型限制。

本产品无法从技术上完全阻止以下行为：
- 用户手动关闭扩展
- 用户卸载扩展
- 用户切换到无扩展的浏览器配置

### 21.2 无法完全防开发者绕过

具有较强技术能力的用户仍可以通过：
- 修改扩展源码
- 调试 storage
- 自行清空状态
来绕过限制。

因此产品定位必须明确为：
- **自律辅助工具**
- 不是强监管安全软件

---

## 22. 推荐工程目录结构

```text
edge-site-blocker/
├── manifest.json
├── src/
│   ├── background/
│   │   └── service-worker.js
│   ├── pages/
│   │   ├── popup/
│   │   │   ├── popup.html
│   │   │   ├── popup.css
│   │   │   └── popup.js
│   │   └── blocked/
│   │       ├── blocked.html
│   │       ├── blocked.css
│   │       └── blocked.js
│   ├── lib/
│   │   ├── storage.js
│   │   ├── rule-engine.js
│   │   ├── captcha.js
│   │   ├── domain.js
│   │   └── constants.js
│   └── assets/
└── README.md
```

如果不想上构建工具，也可以先保持纯原生结构。

---

## 23. 关键纯函数规格

### 23.1 `normalizeDomain(input: string): string`

功能：
- 标准化用户输入域名

行为建议：
- trim
- lowercase
- 去掉协议头
- 去掉尾部斜杠
- 尽量提取 hostname

### 23.2 `isDomainMatched(host: string, ruleDomain: string): boolean`

功能：
- 判断访问域名是否命中规则域名

命中规则：
- 完全相等
- 或 `host.endsWith('.' + ruleDomain)`

### 23.3 `calculateCaptchaLength(level: 1 | 2, minutes: number): number`

功能：
- 根据 level 和申请分钟数计算验证码长度

规则：
- `level = 1`：`5 + 5 * floor((minutes - 5)/2)`
- `level = 2`：`5 + 5 * floor((minutes - 5)/10)`

### 23.4 `generateCaptcha(length: number): string`

功能：
- 生成指定长度随机验证码

要求：
- 使用 `crypto.getRandomValues`
- 最好包含四类字符
- 随机洗牌

### 23.5 `isTemporaryAccessValid(record, now): boolean`

功能：
- 判断某域名临时授权是否仍有效

逻辑：
- `record != null`
- `now < record.expireAt`

---

## 24. 推荐伪代码

### 24.1 导航拦截伪代码

```js
onMainFrameNavigate(url, tabId) {
  const host = extractHost(url)
  const rules = await loadRules()
  const matchedRule = findMatchedEnabledRule(host, rules)

  if (!matchedRule) {
    return allow()
  }

  const access = await getTemporaryAccess(matchedRule.domain)
  const now = Date.now()

  if (access && now < access.expireAt) {
    return allow()
  }

  const blockedUrl = buildBlockedPageUrl({
    target: url,
    domain: matchedRule.domain
  })

  redirectTab(tabId, blockedUrl)
}
```

### 24.2 生成验证码伪代码

```js
function calculateCaptchaLength(level, minutes) {
  if (minutes < 5) throw new Error('minutes must >= 5')

  if (level === 1) {
    return 5 + 5 * Math.floor((minutes - 5) / 2)
  }

  if (level === 2) {
    return 5 + 5 * Math.floor((minutes - 5) / 10)
  }

  throw new Error('level not allowed for captcha')
}
```

```js
function generateCaptcha(length) {
  const upper = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'
  const lower = 'abcdefghijklmnopqrstuvwxyz'
  const digits = '0123456789'
  const symbols = '!@#$%^&*()'
  const all = upper + lower + digits + symbols

  const result = []

  result.push(randomChar(upper))
  result.push(randomChar(lower))
  result.push(randomChar(digits))
  result.push(randomChar(symbols))

  while (result.length < length) {
    result.push(randomChar(all))
  }

  return shuffle(result).join('')
}
```

### 24.3 阻断页申请成功伪代码

```js
async function approveTemporaryAccess(domain, targetUrl, minutes) {
  const now = Date.now()
  const expireAt = now + minutes * 60 * 1000

  await saveTemporaryAccess(domain, {
    startAt: now,
    expireAt,
    grantedAt: now
  })

  location.href = targetUrl
}
```

---

## 25. UI 建议

### 25.1 Popup 页面信息架构

建议模块：
- 顶部标题
- 新增规则区域
- 规则列表区域
- 设置区域（可选，未来放最大申请时长等全局设置）

### 25.2 Blocked 页面信息架构

建议分区：
1. 标题区
2. 当前网站信息区
3. 规则等级说明区
4. 申请区（仅 Level 1/2）
5. 错误提示区

### 25.3 视觉风格建议

- 风格克制、强提示性
- 不要过度娱乐化
- 尽量让用户感受到“有阻力”而非“在玩小游戏”

---

## 26. 测试用例建议

### 26.1 域名匹配测试

1. 规则：`youtube.com`
   - `youtube.com` → 命中
   - `www.youtube.com` → 命中
   - `m.youtube.com` → 命中
   - `notyoutube.com` → 不命中

### 26.2 长度计算测试

#### Level 1
- 5 → 5
- 6 → 5
- 7 → 10
- 9 → 15

#### Level 2
- 5 → 5
- 10 → 5
- 15 → 10
- 25 → 15

### 26.3 验证码生成测试

- 长度正确
- 至少包含四类字符
- 多次生成结果不相同

### 26.4 临时授权测试

- 有效期内访问放行
- 过期后访问阻断

### 26.5 输入限制测试

- 粘贴被阻止
- drop 被阻止
- 自动填充不会污染输入框

---

## 27. 版本里程碑建议

### v0.1 最小可用版本

- 静态规则
- 基础阻断页
- Level 0 / 1 / 2 逻辑
- 临时放行
- 本地存储

### v0.2 可配置版本

- Popup 管理规则
- 增删改查
- 启用/禁用

### v0.3 工程优化版本

- 模块拆分
- 单元测试
- 更完善错误提示
- 更好的输入校验

---

## 28. 给 Codex 的实现要求建议

如果要把本项目交给 Codex 或其他代码生成模型，可以附加以下实现要求：

1. 使用 Manifest V3
2. 使用原生 HTML/CSS/JavaScript，不强依赖前端框架
3. 所有业务逻辑模块化
4. 不使用 `Math.random()` 生成验证码
5. 规则和授权统一存入 `chrome.storage.local`
6. 提供清晰注释
7. 代码需可直接加载为 Edge unpacked extension
8. UI 不追求复杂样式，优先正确性和可维护性
9. 对所有输入做校验
10. 将核心逻辑封装成独立函数，便于单元测试

---

## 29. 可选增强项（非本期）

以下内容不是本期必须，但未来可能有价值：

1. 到期后强制关闭当前受限标签页
2. 申请前增加冷静期倒计时
3. 对不同网站允许不同最大申请时长
4. 导入导出规则
5. 设置页而不只是 popup
6. 黑白主题切换
7. 自定义验证码策略

---

## 30. 最终需求摘要

这个产品是一个 **基于 Edge 扩展的强制型自律网站阻断工具**，其核心逻辑如下：

1. 用户配置受限网站域名及其等级
2. 访问受限网站时默认跳转阻断页
3. 完全禁止的网站永远不可放行
4. 其余网站可申请临时访问
5. 申请时长越长，验证码越复杂
6. 验证码只能手动输入，禁止粘贴
7. 验证通过后写入本地临时授权
8. 在授权过期前允许访问，过期后恢复阻断

该产品的设计目标不是绝对防破解，而是通过高摩擦机制显著降低冲动访问行为。

